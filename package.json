{
    "name": "nacara-dev-tools",
    "private": true,
    "description": "Development tools, and the bundler for the JavaScript that ships inside the packages. Biome formats it; esbuild bundles the default theme's scripts and the live-example client. Nothing here is needed to build a site.",
    "scripts": {
        "format": "biome format --write .",
        "format:check": "biome format ."
    },
    "devDependencies": {
        "@biomejs/biome": "2.5.9",
        "esbuild": "0.28.2"
    },
    "workspaces": [
        "src/Nacara.Plugin.LiveExample"
    ]
}
